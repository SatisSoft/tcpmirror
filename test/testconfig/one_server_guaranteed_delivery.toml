# listen address
listen_address = "localhost:7050"

# DB server address
db_address = "localhost:9999"

#listen protocol
listen_protocol = "NDTP"

#log level
log_level = "trace"

#key expire sec
key_ex = 10

#period check not confirmed ms
period_notconf_data = 60000

#period old data ms
period_old_data = 55000

#period check old data sec
period_check_old = 60

#timeout close sec
timeout_close = 5

#timeout error reply sec
timeout_error_reply = 5

#timeout reconnect sec
timeout_reconnect = 10

# list of data consumers
consumers_list = ["vis1"]

#key expire sec
key_ex = 1

[vis1]
id = 2
address = "localhost:7051"
protocol = "NDTP"
master = true
