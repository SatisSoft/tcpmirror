# listen address
listen_address = "localhost:7010"

# DB server address
db_address = "localhost:9999"

#listen protocol
listen_protocol = "NDTP"

#log level
log_level = "trace"

# list of data consumers
consumers_list = ["vis1"]

# test mode provides more detailed logs but reduces system performance
test_mode = true

#key expire sec
key_ex = 3

[vis1]
id = 2
address = "localhost:7011"
protocol = "NDTP"
master = true
